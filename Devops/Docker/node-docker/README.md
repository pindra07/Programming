# Dockerfile

## How to write a dockerfile
1. Base Image
2. Bunch of commands that you run on the base image (to install dependencies like node.js)
 note:  you can also start from scratch with no base image

## Docker build 
`docker build -t hello-world-app .`
<!-- the dot is the root folder where project have to build inside that container -->

